
# VIBEATHON Rules + Workflow

## Official Rules
Solo Build & Ship | PRD per round | Timestamp ranking | Functionality judging

## Verification → Git (MANDATORY)
**Verified FIRST**, then upload:

**New repo**:
```bash
echo "# vibeathon-harshitsjain-2" > README.md
git init && git add . && git commit -m "Round 2 verified"
git branch -M main
git remote add origin https://github.com/hx010207/vibeathon-harshitsjain-2.git
git push -u origin main
```

## Repo Structure
```
├── index.html    # Live app
├── prd.md       # Product doc
├── rules.md     # This
├── README.md
├── deploy.md
└── verification.md
```
